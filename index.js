let meta = {
    value: 'ler um livro por mês',
    checked: false,
    log: (info) => {
        console.log(info)
    }
}

meta.log(meta.value)

// arrow function
// const criarMeta = () => {}
// named function
// function criarMeta() {}